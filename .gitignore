# runtime data — created at first launch under --data-dir
*.db
*.db-journal
*.log

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
testsprite_tests/tmp/
testsprite_tests/tmp/config.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# internal agent orchestration docs (OMH specs/plans) — not part of the project.
/.omh/
